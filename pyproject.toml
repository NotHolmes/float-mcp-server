[project]
name = "float-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.13",
    "fastmcp>=2.9.2",
    "float-api>=0.0.15",
]

[dependency-groups]
dev = [
    "ruff>=0.12.0",
]
